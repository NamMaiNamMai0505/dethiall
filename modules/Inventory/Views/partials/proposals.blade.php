<div class="overflow-x-auto rounded border bg-white"><table class="w-full text-sm"><thead class="bg-slate-100"><tr><th class="p-3">Loại</th><th class="p-3 text-left">Tiêu đề</th><th class="p-3">Đơn vị</th><th class="p-3">Trạng thái</th><th class="p-3">Quyết định</th></tr></thead><tbody>@foreach($proposals as $proposal)<tr class="border-t"><td class="p-3">{{ $proposal->type }}</td><td class="p-3">{{ $proposal->title }}</td><td class="p-3">{{ $proposal->unit?->name }}</td><td class="p-3">{{ $proposal->status }}</td><td class="p-3">@if($proposal->status==='PENDING')<form method="POST" action="{{ route('inventory.proposals.decide',$proposal) }}" class="flex gap-1">@csrf @method('PATCH')<button name="status" value="APPROVED" class="rounded bg-green-600 px-2 py-1 text-white">Duyệt</button><button name="status" value="REJECTED" class="rounded bg-red-600 px-2 py-1 text-white">Từ chối</button></form>@endif</td></tr>@endforeach</tbody></table></div>
