<div class="rounded border bg-white p-4"><h2 class="mb-2 font-semibold">Nhật ký thao tác</h2>@forelse($auditLogs as $item)<div class="border-b p-2 text-sm">{{ $item->created_at?->format('d/m/Y H:i') }} — {{ $item->action }} — {{ $item->entity_type }} #{{ $item->entity_id }} — {{ $item->user?->name }}</div>@empty<p class="text-slate-500">Chưa có nhật ký nghiệp vụ.</p>@endforelse</div>
