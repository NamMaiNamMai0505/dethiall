<div class="grid gap-4 lg:grid-cols-2"><div class="rounded border bg-white p-4"><h2 class="mb-2 font-semibold">Tài sản trong phòng</h2><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-100"><tr><th class="p-2">Mã</th><th class="p-2">Tên</th><th class="p-2">Danh mục</th><th class="p-2">Trạng thái</th></tr></thead><tbody>@forelse($assets as $asset)<tr class="border-t"><td class="p-2">{{ $asset->asset_code }}</td><td class="p-2">{{ $asset->name }}</td><td class="p-2">{{ $asset->material?->name }}</td><td class="p-2">{{ $asset->status }}</td></tr>@empty<tr><td colspan="4" class="p-4 text-center">Chưa có tài sản.</td></tr>@endforelse</tbody></table></div></div><div class="space-y-4"><form method="POST" action="{{ route('inventory.room.images.store',$classroom) }}" enctype="multipart/form-data" class="rounded border bg-white p-4">@csrf<h2 class="mb-2 font-semibold">Ảnh phòng</h2><input name="image" type="file" accept="image/*" required class="w-full rounded border p-2"><input name="caption" placeholder="Chú thích" class="my-2 w-full rounded border p-2"><button class="rounded bg-blue-600 px-3 py-2 text-white">Tải ảnh lên</button>@foreach($roomImages as $image)<div class="mt-2 flex items-center justify-between border-t pt-2"><a href="{{ asset('storage/'.$image->path) }}" target="_blank">{{ $image->caption ?: basename($image->path) }}</a><form method="POST" action="{{ route('inventory.room.images.delete',$image) }}">@csrf @method('DELETE')<button class="text-red-600">Xóa</button></form></div>@endforeach</form><form method="POST" action="{{ route('inventory.room.users.store',$classroom) }}" class="rounded border bg-white p-4">@csrf<h2 class="mb-2 font-semibold">Người dùng / phụ trách phòng</h2><select name="user_id" required class="w-full rounded border p-2"><option value="">Chọn tài khoản</option>@foreach($users as $user)<option value="{{ $user->id }}">{{ $user->name }}</option>@endforeach</select><input name="role" placeholder="Vai trò" class="my-2 w-full rounded border p-2"><button class="rounded bg-blue-600 px-3 py-2 text-white">Gán phụ trách</button>@foreach($roomUsers as $roomUser)<div class="mt-2 flex justify-between border-t pt-2"><span>{{ $roomUser->user?->name }} — {{ $roomUser->role }}</span><form method="POST" action="{{ route('inventory.room.users.delete',$roomUser) }}">@csrf @method('DELETE')<button class="text-red-600">Bỏ</button></form></div>@endforeach</form></div></div>
