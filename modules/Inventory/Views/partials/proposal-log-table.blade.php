<div id="inventory-log-proposals" class="mt-4 overflow-x-auto rounded border bg-white p-4"><h2 class="mb-3 font-semibold">Đề xuất / thanh lý</h2><table class="w-full min-w-[1100px] text-left text-sm"><thead class="bg-slate-100"><tr><th class="p-3">Thời gian</th><th class="p-3">Vật tư</th><th class="p-3">Số lượng</th><th class="p-3">Lý do</th><th class="p-3">Đơn vị đề xuất</th><th class="p-3">Người duyệt</th><th class="p-3">Số quyết định</th><th class="p-3">Trạng thái</th></tr></thead><tbody>@forelse($proposalLogs as $proposal)@php($item=$proposal->items->first())<tr class="border-t"><td class="p-3">{{ $proposal->created_at?->format('d/m/Y H:i') }}</td><td class="p-3">{{ $item?->material_code ?: $item?->original_code ?: '—' }} — {{ $item?->material_name ?: $item?->name ?: $proposal->title }}</td><td class="p-3">{{ (int)($item?->quantity ?: 0) }}</td><td class="p-3">{{ $proposal->description ?: $item?->note ?: '—' }}</td><td class="p-3">{{ $proposal->unit?->name ?: '—' }}</td><td class="p-3">{{ $proposal->decidedBy?->name ?: '—' }}</td><td class="p-3">{{ $proposal->decision_number ?: '—' }}</td><td class="p-3">{{ ['PENDING'=>'Chờ duyệt','APPROVED'=>'Đã duyệt','REJECTED'=>'Từ chối','COMPLETED'=>'Đã hoàn thành'][$proposal->status] ?? $proposal->status }}</td></tr>@empty<tr><td colspan="8" class="p-5 text-center text-slate-500">Chưa có đề xuất / thanh lý.</td></tr>@endforelse</tbody></table></div>
