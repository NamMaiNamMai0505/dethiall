<div id="inventory-log-transfers" class="mt-4 overflow-x-auto rounded border bg-white p-4"><h2 class="mb-3 font-semibold">Điều động / thu hồi</h2><table class="w-full min-w-[1200px] text-left text-sm"><thead class="bg-slate-100"><tr><th class="p-3">Thời gian</th><th class="p-3">Vật tư</th><th class="p-3">Số lượng</th><th class="p-3">Phòng nguồn</th><th class="p-3">Phòng đích</th><th class="p-3">Đơn vị thực hiện</th><th class="p-3">Số quyết định</th><th class="p-3">Trạng thái</th></tr></thead><tbody>@forelse($transferLogs as $item)<tr class="border-t"><td class="p-3">{{ ($item->performed_at ?: $item->created_at)?->format('d/m/Y H:i') }}</td><td class="p-3">{{ $item->asset?->asset_code ?: $item->material?->code ?: '—' }} — {{ $item->asset?->name ?: $item->material?->name ?: '—' }}</td><td class="p-3">{{ (int)($item->asset?->quantity ?: $item->material?->quantity ?: 0) }}</td><td class="p-3">{{ $item->fromClassroom?->name ?: '—' }}</td><td class="p-3">{{ $item->type === 'RECALL' ? ($item->warehouse?->name ?: 'Kho vật tư') : ($item->toClassroom?->name ?: '—') }}</td><td class="p-3">{{ $item->performing_unit ?: '—' }}</td><td class="p-3">{{ $item->decision_number ?: '—' }}</td><td class="p-3">{{ ['PENDING'=>'Chờ duyệt','APPROVED'=>'Đã duyệt','REJECTED'=>'Từ chối','COMPLETED'=>'Đã hoàn thành'][$item->status] ?? $item->status }}</td></tr>@empty<tr><td colspan="8" class="p-5 text-center text-slate-500">Chưa có phiếu điều động / thu hồi.</td></tr>@endforelse</tbody></table></div>
