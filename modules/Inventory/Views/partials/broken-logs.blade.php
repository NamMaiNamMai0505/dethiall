<div class="rounded border bg-white p-4"><h2 class="mb-2 font-semibold">Nhật ký hỏng / sửa chữa</h2>@forelse($brokenLogs as $item)<div class="border-b p-2 text-sm">{{ $item->event_at?->format('d/m/Y H:i') }} — {{ $item->asset_code ?: '—' }} — {{ ['BROKEN'=>'Báo hỏng','COMPLETED'=>'Hoàn thành sửa chữa'][$item->event_type] ?? $item->event_type }} — {{ ['REPAIRING'=>'Đang sửa chữa','NORMAL'=>'Bình thường','BROKEN'=>'Hỏng'][$item->status_after] ?? $item->status_after }} — {{ $item->reason ?: $item->result_note ?: '—' }}</div>@empty<p class="text-slate-500">Chưa có nhật ký hỏng.</p>@endforelse</div>
