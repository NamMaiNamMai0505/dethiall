<div class="space-y-5">
    <div class="rounded-2xl border border-blue-100 bg-gradient-to-r from-blue-50 to-white p-5 shadow-sm"><div class="flex items-start gap-3"><span class="flex h-11 w-11 items-center justify-center rounded-xl bg-blue-700 text-xl text-white shadow-md"><i class="bi bi-people-fill"></i></span><div><h2 class="text-lg font-extrabold text-slate-900">Thêm lớp thuộc Đại đội</h2><p class="mt-1 text-sm font-medium text-slate-500">Mỗi lớp phải thuộc một Đại đội; Đại đội đã thuộc Tiểu đoàn quản lý.</p></div></div><form method="POST" action="{{ route('leave-management.classes.store') }}" class="mt-5 grid gap-4 md:grid-cols-3">@csrf<label class="block md:col-span-1"><span class="mb-1.5 block text-sm font-bold text-slate-700">Đại đội quản lý <b class="text-rose-500">*</b></span><select name="unit_id" required class="ui-select w-full rounded-xl border border-slate-200 px-3 py-2.5"><option value="">Chọn Đại đội</option>@foreach($units as $unit)<option value="{{ $unit->id }}">{{ $unit->code }} — {{ $unit->name }} @if($unit->parent) ({{ $unit->parent->name }}) @endif</option>@endforeach</select></label><label class="block md:col-span-1"><span class="mb-1.5 block text-sm font-bold text-slate-700">Tên lớp <b class="text-rose-500">*</b></span><input name="name" required placeholder="Ví dụ: Lớp A1" class="w-full rounded-xl border border-slate-200 px-3 py-2.5"></label><div class="flex items-end"><button class="w-full rounded-xl bg-blue-700 px-4 py-2.5 font-bold text-white shadow-md shadow-blue-100 hover:bg-blue-800"><i class="bi bi-plus-lg mr-1"></i>Thêm lớp</button></div></form></div>
    <div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="mb-4 flex items-center justify-between"><div><h2 class="text-lg font-extrabold text-slate-900">Danh sách lớp</h2><p class="mt-1 text-sm text-slate-500">Các lớp được phân theo đúng Đại đội quản lý.</p></div><span class="rounded-full bg-emerald-100 px-3 py-1 text-xs font-bold text-emerald-800">{{ $items->count() }} lớp</span></div><div class="grid gap-3 md:grid-cols-2 xl:grid-cols-3">@forelse($items as $item)<div class="rounded-xl border border-emerald-100 bg-emerald-50/50 p-4"><div class="flex items-start justify-between gap-3"><div><p class="text-xs font-bold uppercase tracking-wider text-emerald-700">{{ $item->unit?->name ?: 'Chưa gán Đại đội' }}</p><h3 class="mt-1 text-lg font-extrabold text-slate-900">{{ $item->name }}</h3><p class="mt-1 text-sm text-slate-500">{{ $item->unit?->parent?->name ?: 'Chưa có Tiểu đoàn' }}</p></div><i class="bi bi-people text-xl text-emerald-600"></i></div><div class="mt-4 flex items-center justify-between border-t border-emerald-100 pt-3"><span class="text-xs font-semibold text-slate-500">{{ $item->personnel_count ?? $item->personnel()->count() }} học viên</span><form method="POST" action="{{ route('leave-management.classes.delete',$item) }}" onsubmit="return confirm('Xóa lớp này?');">@csrf @method('DELETE')<button class="text-xs font-bold text-rose-600 hover:text-rose-800">Xóa lớp</button></form></div></div>@empty<div class="rounded-xl border border-dashed border-slate-300 p-8 text-center text-slate-500 md:col-span-2 xl:col-span-3"><i class="bi bi-people text-3xl text-slate-300"></i><p class="mt-2 font-semibold">Chưa có lớp nào</p></div>@endforelse</div></div>
</div>
