<div class="rounded border bg-white p-4">@foreach($items as $item)<div class="border-b py-2"><form method="POST" action="{{ route('leave-management.units.update',$item) }}" class="flex flex-wrap gap-1">@csrf @method('PATCH')<input name="code" value="{{ $item->code }}" required class="w-24 rounded border p-1"><input name="name" value="{{ $item->name }}" required class="flex-1 rounded border p-1"><input name="level" type="number" min="1" value="{{ $item->level }}" class="w-16 rounded border p-1"><input type="hidden" name="status" value="{{ $item->status }}"><button class="rounded bg-blue-600 px-2 py-1 text-xs text-white">Lưu</button></form><form method="POST" action="{{ route('leave-management.units.delete',$item) }}" class="mt-1">@csrf @method('DELETE')<button class="text-xs text-red-600">Xóa đơn vị</button></form></div>@endforeach</div>
