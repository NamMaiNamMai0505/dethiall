<div class="space-y-3"><form method="POST" action="{{ route('leave-management.units.store') }}" class="grid gap-2 rounded border bg-white p-4 md:grid-cols-5">@csrf<input name="code" required placeholder="Mã đơn vị" class="rounded border p-2"><input name="name" required placeholder="Tên đơn vị" class="rounded border p-2"><select name="parent_id" class="rounded border p-2"><option value="">Đơn vị cấp trên</option>@foreach($parents as $parent)<option value="{{ $parent->id }}">{{ $parent->name }}</option>@endforeach</select><input name="level" type="number" min="1" value="1" class="rounded border p-2"><select name="status" class="rounded border p-2"><option value="active">Hoạt động</option><option value="inactive">Tạm ngừng</option></select><button class="rounded bg-blue-600 px-4 py-2 text-white md:col-span-5">Thêm đơn vị</button></form><form method="POST" action="{{ route('leave-management.units.import') }}" enctype="multipart/form-data" class="flex flex-wrap gap-2 rounded border bg-white p-3">@csrf<input name="file" type="file" accept=".xlsx,.xls,.csv" required class="rounded border p-2"><button class="rounded bg-slate-700 px-3 py-2 text-white">Import đơn vị</button></form><form method="POST" action="{{ route('leave-management.units.seed') }}" class="rounded border bg-white p-3">@csrf<button class="rounded border px-3 py-2">Đồng bộ đơn vị hệ thống</button></form></div>
