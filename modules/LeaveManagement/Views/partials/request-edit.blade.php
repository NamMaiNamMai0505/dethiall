@if(in_array($request->status,['PENDING','DRAFT','PENDING_COMMANDER'],true))<form method="POST" action="{{ route('leave-management.requests.update',$request) }}" class="mt-4 grid gap-2 rounded border bg-white p-4 md:grid-cols-4">@csrf @method('PATCH')<input name="from_date" type="date" value="{{ $request->from_date?->format('Y-m-d') }}" class="rounded border p-2"><input name="to_date" type="date" value="{{ $request->to_date?->format('Y-m-d') }}" class="rounded border p-2"><input name="travel_days" type="number" min="0" value="{{ $request->travel_days }}" class="rounded border p-2"><input name="extra_days" type="number" min="0" value="{{ $request->extra_days }}" class="rounded border p-2"><textarea name="reason" class="rounded border p-2 md:col-span-4">{{ $request->reason }}</textarea><button class="rounded bg-blue-600 px-4 py-2 text-white">Cập nhật đơn</button></form>@endif
