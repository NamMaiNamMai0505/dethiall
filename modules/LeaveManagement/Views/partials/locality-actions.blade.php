<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="flex flex-col gap-3 border-b border-slate-100 bg-gradient-to-r from-blue-50 to-white px-5 py-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 class="text-lg font-extrabold text-slate-900">Danh sách địa phương</h2><p class="text-sm font-medium text-slate-500">Đối chiếu đúng dữ liệu tỉnh/thành phố và phường/xã từ file import.</p></div><span class="rounded-full bg-blue-100 px-3 py-1 text-xs font-bold text-blue-800">{{ count($items) }} bản ghi</span></div><div class="overflow-x-auto"><table class="w-full min-w-[900px] text-left text-sm"><thead class="bg-slate-50 text-xs font-extrabold uppercase tracking-wide text-slate-600"><tr><th class="w-16 px-5 py-3">STT</th><th class="px-5 py-3">Tên tỉnh/TP</th><th class="px-5 py-3">Mã tỉnh (TMS)</th><th class="px-5 py-3">Mã phường/xã</th><th class="px-5 py-3">Tên Phường/Xã</th><th class="px-5 py-3 text-right">Thao tác</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($items as $item)@php($isProvince=strtoupper((string)$item->level)==='PROVINCE')<tr class="transition hover:bg-blue-50/50"><td class="px-5 py-3 font-bold text-slate-500">{{ $loop->iteration }}</td><td class="px-5 py-3 font-bold text-slate-800">{{ $isProvince ? $item->name : ($item->parent?->name ?: '—') }}</td><td class="px-5 py-3 font-mono text-xs font-bold text-blue-800">{{ $isProvince ? ($item->code ?: '—') : ($item->parent?->code ?: '—') }}</td><td class="px-5 py-3 font-mono text-xs font-bold text-blue-800">{{ $isProvince ? '—' : ($item->code ?: '—') }}</td><td class="px-5 py-3 font-semibold text-slate-700">{{ $isProvince ? '—' : $item->name }}</td><td class="px-5 py-3 text-right"><div class="inline-flex items-center gap-2"><details class="relative text-left"><summary class="cursor-pointer list-none rounded-lg border border-blue-200 bg-white px-3 py-1.5 text-xs font-bold text-blue-700 hover:bg-blue-700 hover:text-white">Sửa</summary><div class="absolute right-0 z-20 mt-2 w-72 rounded-xl border border-slate-200 bg-white p-3 shadow-xl"><form method="POST" action="{{ route('leave-management.localities.update',$item) }}" class="grid gap-2">@csrf @method('PATCH')<label class="text-xs font-bold text-slate-600">Tên</label><input name="name" value="{{ $item->name }}" class="rounded-lg border p-1.5"><label class="text-xs font-bold text-slate-600">Cấp</label><input name="level" value="{{ $item->level }}" class="rounded-lg border p-1.5"><label class="text-xs font-bold text-slate-600">Mã</label><input name="code" value="{{ $item->code }}" class="rounded-lg border p-1.5"><button class="rounded-lg bg-blue-700 px-3 py-2 text-xs font-bold text-white">Lưu thay đổi</button></form></div></details>@if(!$item->children()->exists())<form method="POST" action="{{ route('leave-management.localities.delete',$item) }}">@csrf @method('DELETE')<button class="rounded-lg border border-rose-200 px-3 py-1.5 text-xs font-bold text-rose-600 hover:bg-rose-600 hover:text-white">Xóa</button></form>@endif</div></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center"><i class="bi bi-geo-alt text-3xl text-slate-300"></i><p class="mt-2 font-bold text-slate-700">Chưa có địa phương</p><p class="mt-1 text-sm text-slate-500">Hãy import file có đúng 5 cột như tiêu đề phía trên.</p></td></tr>@endforelse</tbody></table></div></div>
